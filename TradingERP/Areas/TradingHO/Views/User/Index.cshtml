@model IEnumerable<TradingERP.Models.UserMaster>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/TradingHO/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <h1>Users</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table bg-white">
        <thead>
            <tr>
               
                <th>
                    @Html.DisplayNameFor(model => model.UsmName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UsmEmail)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UsmContact)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UsmPassword)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UsmStatus)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    
                    <td>
                        @Html.DisplayFor(modelItem => item.UsmName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsmEmail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsmContact)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsmPassword)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsmStatus)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>