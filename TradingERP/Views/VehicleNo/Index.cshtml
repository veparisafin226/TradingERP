@model IEnumerable<TradingERP.Models.VehicleNoMaster>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container-fluid">

    <h1>Drivers</h1>

    <p class="float-right btn btn-primary">
        <a class="text-white" asp-action="Create">Create New</a>
    </p>
    <table class="table bg-white p-2">
        <thead>
            <tr>
               
                <th>
                    @Html.DisplayNameFor(model => model.VnmNo)
                </th>
               
                <th>
                    @Html.DisplayNameFor(model => model.VnmStatus)
                </th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                   
                    <td>
                        @Html.DisplayFor(modelItem => item.VnmNo)
                    </td>
                   
                    <td>
                        @Html.DisplayFor(modelItem => item.VnmStatus)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new {  id=item.VnmId}) |

                        @Html.ActionLink("Delete", "Delete", new { id = item.VnmId }, new
                            {
                                onclick = "return confirm('Are you sure you want to delete this record?');"
                            })
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>

@section Scripts {
   
}